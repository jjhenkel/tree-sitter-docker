===================================
Tests for '_anything'
===================================

RUN '#this is a test'

ENV name \

# Testing
bar=foo

ENV test 

MAINTAINER test # test

RUN test \

\
testing

---

(dockerfile
    (run)
    (env (env_pair (env_key) (line_continuation) (comment) (env_value)))
    (env (env_key))
    (maintainer (comment))
    (run (line_continuation) (line_continuation))
) 
